[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "pneumonia-ai"
version = "0.1.0"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
include = ["pneumonia_ai*"]

[tool.pytest.ini_options]
pythonpath = ["src"]
